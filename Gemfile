gemspec

group :test do
  source 'https://rubygems.org' do
    gem 'minitest'
    gem 'rake', '~> 12.3'
    gem 'travis'
    gem 'codecov', :require => false, :group => :test
  end
end

group :development do
  source 'https://rubygems.org' do
    gem 'gem-release'
  end
end
